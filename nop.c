#include "monty.h"

/**
  * nop - function that does nothing
  * @stack: the stack thats not working
  * @line_cnt: the line number which not working you
  */

void nop(stack_t **stack, unsigned int line_cnt)
{
	(void) stack;
	(void) line_cnt;
}
